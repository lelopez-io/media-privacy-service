[tools]
go = "1.22"

[tasks.setup]
description = "First-time setup: install tools, dependencies, and git hooks"
run = [
  "mise install",
  "ln -sf ../../.githooks/post-commit .git/hooks/post-commit",
  "chmod +x .githooks/post-commit",
  "go mod download"
]
